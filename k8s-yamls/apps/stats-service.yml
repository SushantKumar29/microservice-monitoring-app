apiVersion: v1
kind: Service
metadata:
  name: stats-service
  labels:
    has-metrics: 'true' # For Prometheus
spec:
  type: ClusterIP
  selector:
    app: monitoring-app
    component: stats
  ports:
    - port: 80
      targetPort: 3002
      name: http
