apiVersion: v1
kind: Service
metadata:
  name: worker-service
  labels:
    has-metrics: "true" # For Prometheus
spec:
  type: ClusterIP
  selector:
    app: monitoring-app
    component: worker
  ports:
    - port: 80
      targetPort: 3001
      name: http
