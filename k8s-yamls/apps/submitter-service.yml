apiVersion: v1
kind: Service
metadata:
  name: submitter-service
spec:
  type: LoadBalancer
  selector:
    app: monitoring-app
    component: submitter
  ports:
    - port: 80
      targetPort: 3000
