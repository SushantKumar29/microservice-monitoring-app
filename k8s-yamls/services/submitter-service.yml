apiVersion: v1
kind: Service
metadata:
  name: submitter-service
  labels:
    name: submitter-service
    app: monitoring-app
spec:
  type: NodePort
  selector:
    name: submitter-pod
    app: monitoring-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30001
