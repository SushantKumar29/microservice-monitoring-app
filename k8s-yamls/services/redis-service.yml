apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    name: redis-service
    app: monitoring-app
spec:
  type: ClusterIP
  selector:
    name: redis-pod
    app: monitoring-app
  ports:
    - port: 6379
      targetPort: 6379
